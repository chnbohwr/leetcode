const func = require('../src/3sum-closest');

describe('3sum-closest', () => {
  test('should ', () => {
    expect(func([-1, 2, 1, -4], 1)).toBe(2)
  });
  test('should ', () => {
    expect(func([1, 1, 1, 1], 0)).toBe(3)
  });
});